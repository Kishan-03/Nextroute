"use client";
import { useState } from "react";
import { ArrowLeft, BriefcaseBusiness, MapPin, Search, Sparkles } from "lucide-react";
const jobs = [{ title: "Frontend Engineer Intern", company: "Nava Fintech", location: "Bengaluru · Hybrid", match: 92, type: "Internship" }, { title: "Cloud Engineering Apprentice", company: "Bharat Cloud Systems", location: "Bengaluru · On-site", match: 84, type: "Apprenticeship" }, { title: "React Developer", company: "Mitra Mobility", location: "Pune · Remote", match: 78, type: "Full-time" }];
export default function OpportunitiesPage() { const [query, setQuery] = useState(""); const [saved, setSaved] = useState<string[]>([]); const visible = jobs.filter((job) => `${job.title} ${job.company} ${job.location}`.toLowerCase().includes(query.toLowerCase())); return <main className="min-h-screen bg-[#090b16] p-5 text-white sm:p-10"><div className="mx-auto max-w-5xl"><a href="/" className="mb-8 flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/> Dashboard</a><div className="mb-8"><p className="mb-2 text-sm text-amber-300">Industry opportunities</p><h1 className="text-4xl font-bold">Find your next opportunity</h1><p className="mt-2 text-slate-400">Personalized jobs, internships, and apprenticeships ranked by your skills.</p></div><div className="relative mb-6"><Search className="absolute left-3 top-3 text-slate-500" size={18}/><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full rounded-xl border border-white/10 bg-white/5 py-3 pl-10 text-sm outline-none focus:border-violet-400" placeholder="Search roles, companies, or locations"/></div><div className="space-y-4">{visible.map((job) => <article key={job.title} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex gap-4"><div className="rounded-xl bg-violet-500/15 p-3"><BriefcaseBusiness className="text-violet-300"/></div><div><h2 className="font-semibold">{job.title}</h2><p className="mt-1 text-sm text-slate-400">{job.company}</p><p className="mt-2 flex items-center gap-1 text-xs text-slate-500"><MapPin size={13}/>{job.location} · {job.type}</p></div></div><div className="flex items-center gap-3"><span className="rounded-full bg-emerald-400/15 px-3 py-1.5 text-xs text-emerald-300">{job.match}% match</span><button onClick={() => setSaved((current) => current.includes(job.title) ? current.filter((item) => item !== job.title) : [...current, job.title])} className="rounded-xl border border-white/10 px-4 py-2 text-sm">{saved.includes(job.title) ? "Saved" : "Save"}</button><button className="rounded-xl bg-violet-500 px-4 py-2 text-sm">Apply</button></div></div></article>)}</div>{visible.length === 0 && <div className="rounded-2xl border border-dashed border-white/15 p-12 text-center text-slate-500"><Sparkles className="mx-auto mb-3 text-violet-300"/>No matching opportunities found.</div>}</div></main>; }
