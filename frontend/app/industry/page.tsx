"use client";
import { useEffect, useState } from "react";
import { ArrowLeft, BriefcaseBusiness, Building2, Map, MessageSquare, ShieldCheck, Sparkles, Target, TrendingUp } from "lucide-react";

const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:4000/api";
type Region = { state: string; district: string; industry: string; enrollments: number; openRoles: number; shortageIndex: number; medianSalary: number };
type Opportunity = { companyName: string; title: string; district: string; score: number; nearby: boolean; stipend: number; openings: number };

export default function IndustryPage() {
  const [regions, setRegions] = useState<Region[]>([]); const [opportunities, setOpportunities] = useState<Opportunity[]>([]); const [industry, setIndustry] = useState("ALL"); const [message, setMessage] = useState("");
  const notify = (text: string) => { setMessage(text); window.setTimeout(() => setMessage(""), 2400); };
  useEffect(() => { void loadRegions("ALL"); void loadMatches(); }, []);
  async function loadRegions(value: string) { setIndustry(value); const response = await fetch(`${api}/industry/heatmap?industry=${encodeURIComponent(value)}`); const data = await response.json() as { regions: Region[] }; setRegions(data.regions); }
  async function loadMatches() { const response = await fetch(`${api}/industry/apprenticeships/matches?skills=React,TypeScript`); const data = await response.json() as { opportunities: Opportunity[] }; setOpportunities(data.opportunities); }
  return <main className="min-h-screen bg-[#090b16] p-5 text-white sm:p-10">{message && <div className="fixed right-5 top-5 z-30 rounded-xl bg-violet-500 px-4 py-3 text-sm">{message}</div>}<div className="mx-auto max-w-7xl"><div className="mb-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm text-cyan-300">Module 3 · Industry intelligence</p><h1 className="text-3xl font-bold sm:text-4xl">India’s talent <span className="text-cyan-300">pulse</span></h1><p className="mt-2 text-slate-400">Connect regional demand, employers, and student opportunity.</p></div><a href="/" className="flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/> Dashboard</a></div><div className="mb-6 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-cyan-400/20 bg-cyan-400/10 p-5"><Map className="mb-4 text-cyan-300" size={21}/><p className="text-sm text-slate-400">Regions tracked</p><p className="mt-1 text-3xl font-bold">28 states</p></div><div className="rounded-2xl border border-violet-400/20 bg-violet-400/10 p-5"><BriefcaseBusiness className="mb-4 text-violet-300" size={21}/><p className="text-sm text-slate-400">Open demand</p><p className="mt-1 text-3xl font-bold">1.84M roles</p></div><div className="rounded-2xl border border-amber-400/20 bg-amber-400/10 p-5"><TrendingUp className="mb-4 text-amber-300" size={21}/><p className="text-sm text-slate-400">Average shortage</p><p className="mt-1 text-3xl font-bold">71%</p></div></div><div className="grid gap-6 xl:grid-cols-[1.35fr_1fr]"><section className="rounded-2xl border border-white/10 bg-white/[.04] p-6"><div className="mb-6 flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h2 className="flex items-center gap-2 font-semibold"><Map size={18} className="text-cyan-300"/> Live India heatmap</h2><p className="mt-1 text-sm text-slate-500">Demand vs. student supply by region</p></div><select value={industry} onChange={(event) => void loadRegions(event.target.value)} className="rounded-lg border border-white/10 bg-white/5 p-2 text-xs text-slate-300"><option value="ALL">All industries</option><option value="Technology">Technology</option><option value="Manufacturing">Manufacturing</option><option value="Automotive">Automotive</option><option value="Services">Services</option></select></div><div className="mb-6 grid grid-cols-2 gap-2 sm:grid-cols-3">{regions.map((region) => <button key={`${region.state}-${region.district}`} onClick={() => notify(`${region.district}: ${region.shortageIndex}% skill shortage`)} className="rounded-xl border border-white/10 bg-gradient-to-br from-cyan-400/20 to-violet-400/10 p-4 text-left transition hover:border-cyan-300"><p className="text-sm font-medium">{region.state}</p><p className="mt-1 text-xs text-slate-500">{region.district}</p><div className="mt-4 flex items-end justify-between"><span className="text-xl font-bold text-cyan-200">{region.shortageIndex}%</span><span className="text-[10px] text-slate-500">shortage</span></div><div className="mt-2 h-1.5 rounded-full bg-white/10"><div className="h-full rounded-full bg-cyan-300" style={{ width: `${region.shortageIndex}%` }}/></div></button>)}</div><div className="grid gap-3 text-sm sm:grid-cols-3"><div className="rounded-xl bg-white/5 p-4"><p className="text-slate-500">Enrollments</p><p className="mt-1 font-semibold">{regions.reduce((sum, region) => sum + region.enrollments, 0).toLocaleString("en-IN")}</p></div><div className="rounded-xl bg-white/5 p-4"><p className="text-slate-500">Job demand</p><p className="mt-1 font-semibold">{regions.reduce((sum, region) => sum + region.openRoles, 0).toLocaleString("en-IN")}</p></div><div className="rounded-xl bg-white/5 p-4"><p className="text-slate-500">Median salary</p><p className="mt-1 font-semibold">₹{Math.round(regions.reduce((sum, region) => sum + region.medianSalary, 0) / Math.max(regions.length, 1) / 1000)}k</p></div></div></section><section className="rounded-2xl border border-white/10 bg-white/[.04] p-6"><div className="mb-6 flex items-center justify-between"><div><h2 className="flex items-center gap-2 font-semibold"><Target size={18} className="text-violet-300"/> Apprenticeship matcher</h2><p className="mt-1 text-sm text-slate-500">Matched to your React + TypeScript profile</p></div><Sparkles size={18} className="text-violet-300"/></div><div className="space-y-3">{opportunities.map((opportunity) => <button key={opportunity.title} onClick={() => notify(`Application interest recorded for ${opportunity.title}`)} className="w-full rounded-xl border border-white/10 bg-white/[.03] p-4 text-left hover:border-violet-400/60"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{opportunity.title}</p><p className="mt-1 text-xs text-slate-500">{opportunity.companyName} · {opportunity.district}</p></div><span className="rounded-full bg-emerald-400/15 px-2 py-1 text-xs text-emerald-300">{opportunity.score}% match</span></div><div className="mt-4 flex justify-between text-xs text-slate-500"><span>₹{opportunity.stipend.toLocaleString("en-IN")} / month</span><span>{opportunity.openings} openings</span></div></button>)}</div><button onClick={() => notify("More nearby opportunities are being matched")} className="mt-5 w-full rounded-xl border border-white/10 py-2.5 text-sm text-slate-300 hover:bg-white/5">Find more opportunities</button></section></div><div className="mt-6 grid gap-6 md:grid-cols-3"><button onClick={() => notify("Collaboration request form opened")} className="rounded-2xl border border-white/10 bg-white/[.04] p-6 text-left hover:border-violet-400"><Building2 className="mb-4 text-violet-300"/><h3 className="font-semibold">Industry collaboration</h3><p className="mt-2 text-sm text-slate-500">Approve skills, sponsor courses, and publish internships.</p></button><button onClick={() => notify("MSME survey link copied")} className="rounded-2xl border border-white/10 bg-white/[.04] p-6 text-left hover:border-cyan-400"><MessageSquare className="mb-4 text-cyan-300"/><h3 className="font-semibold">MSME demand survey</h3><p className="mt-2 text-sm text-slate-500">Collect hiring demand through WhatsApp and SMS workflows.</p></button><button onClick={() => notify("Skill bounty marketplace opened")} className="rounded-2xl border border-white/10 bg-white/[.04] p-6 text-left hover:border-amber-400"><ShieldCheck className="mb-4 text-amber-300"/><h3 className="font-semibold">Skill bounty marketplace</h3><p className="mt-2 text-sm text-slate-500">Complete company tasks, earn rewards, and get hired.</p></button></div></div></main>;
}
