"use client";
import { useState } from "react";
import { ArrowLeft, MessageCircle, Send, Users } from "lucide-react";
export default function CommunityPage() { const [text, setText] = useState(""); const [posts, setPosts] = useState(["How are you preparing for the next system design assessment?", "Sharing a great open-source contribution guide for beginners."]); const submit = () => { if (text.trim()) { setPosts((current) => [text.trim(), ...current]); setText(""); } }; return <main className="min-h-screen bg-[#090b16] p-5 text-white sm:p-10"><div className="mx-auto max-w-4xl"><a href="/" className="mb-8 flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/> Dashboard</a><div className="mb-8"><p className="mb-2 text-sm text-emerald-300">SkillSync community</p><h1 className="text-4xl font-bold">Learn together</h1><p className="mt-2 text-slate-400">Ask questions, share projects, and find peers working toward the same roles.</p></div><div className="mb-6 rounded-2xl border border-white/10 bg-white/[.04] p-5"><textarea value={text} onChange={(event) => setText(event.target.value)} className="min-h-24 w-full resize-none bg-transparent text-sm outline-none" placeholder="Share a question or learning win..."/><div className="flex justify-end"><button onClick={submit} className="flex items-center gap-2 rounded-xl bg-violet-500 px-4 py-2 text-sm"><Send size={15}/> Post</button></div></div><div className="space-y-3">{posts.map((post, index) => <article key={`${post}-${index}`} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="mb-3 flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-gradient-to-br from-violet-400 to-cyan-400 text-xs font-bold text-slate-950">{index ? "RS" : "AK"}</div><div><p className="text-sm font-medium">{index ? "Riya Sharma" : "Aarav Kapoor"}</p><p className="text-xs text-slate-500">{index + 1}h ago</p></div></div><p className="text-sm text-slate-300">{post}</p><div className="mt-4 flex gap-4 text-xs text-slate-500"><button className="flex items-center gap-1 hover:text-white"><MessageCircle size={14}/> Reply</button><button className="flex items-center gap-1 hover:text-white"><Users size={14}/> Join discussion</button></div></article>)}</div></div></main>; }
