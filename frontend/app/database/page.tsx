"use client";
import { useEffect, useState } from "react";
import { ArrowLeft, Database, RefreshCw, ShieldCheck, Users } from "lucide-react";

type UserRecord = { id: string; name: string; email: string; role: string };
type DataResponse = { storage: string; users: UserRecord[]; note: string };
export default function DatabasePage() { const [data, setData] = useState<DataResponse>(); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); async function load() { setLoading(true); try { const response = await fetch("http://localhost:4000/api/dev/data"); if (!response.ok) throw new Error("Database data unavailable"); setData(await response.json() as DataResponse); } catch (err) { setError(err instanceof Error ? err.message : "Unable to load data"); } finally { setLoading(false); } } useEffect(() => { void load(); }, []); return <main className="min-h-screen bg-[#090b16] p-5 text-white sm:p-10"><div className="mx-auto max-w-6xl"><div className="mb-8 flex items-center justify-between"><div><p className="mb-2 text-sm text-cyan-300">SkillSync AI · Local data console</p><h1 className="text-3xl font-bold">Database dashboard</h1><p className="mt-2 text-sm text-slate-400">Development data overview with sensitive password hashes hidden.</p></div><a href="/" className="flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/> Dashboard</a></div>{loading && <div className="panel">Loading database records...</div>}{error && <div className="rounded-2xl border border-rose-400/20 bg-rose-400/10 p-5 text-rose-300">{error}</div>}{data && <><div className="grid gap-4 sm:grid-cols-3"><div className="stat"><Database className="text-cyan-300"/><p>Storage</p><strong className="text-lg">{data.storage}</strong></div><div className="stat"><Users className="text-violet-300"/><p>Users</p><strong>{data.users.length}</strong></div><div className="stat"><ShieldCheck className="text-emerald-300"/><p>Security</p><strong className="text-lg">Hashes hidden</strong></div></div><section className="panel mt-6 overflow-x-auto"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-xl font-semibold">Users table</h2><p className="mt-1 text-sm text-slate-500">Persistent local accounts</p></div><button onClick={() => void load()} className="flex items-center gap-2 rounded-xl border border-white/10 px-3 py-2 text-sm text-slate-300 hover:bg-white/5"><RefreshCw size={15}/> Refresh</button></div><table className="w-full min-w-[600px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase text-slate-500"><tr><th className="px-3 py-3">ID</th><th className="px-3 py-3">Name</th><th className="px-3 py-3">Email</th><th className="px-3 py-3">Role</th></tr></thead><tbody>{data.users.map((user) => <tr className="border-b border-white/5" key={user.id}><td className="px-3 py-4 font-mono text-xs text-slate-500">{user.id}</td><td className="px-3 py-4">{user.name}</td><td className="px-3 py-4 text-slate-300">{user.email}</td><td className="px-3 py-4"><span className="rounded-full bg-violet-400/15 px-3 py-1 text-xs text-violet-200">{user.role}</span></td></tr>)}</tbody></table>{data.users.length === 0 && <p className="py-10 text-center text-slate-500">No users found. Create an account from the login page.</p>}<p className="mt-5 text-xs text-slate-600">{data.note}</p></section></>}</div></main>; }
